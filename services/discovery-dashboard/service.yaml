apiVersion: v1
kind: Service
metadata:
  name: discovery-dashboard
  namespace: homelab
spec:
  selector:
    app: discovery-dashboard
  ports:
  - port: 80
    targetPort: 80
    name: http
  type: NodePort

apiVersion: v1
kind: Service
metadata:
  name: homelab-dashboard
  namespace: homelab
  labels:
    app: homelab-dashboard
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30800
    protocol: TCP
    name: http
  selector:
    app: homelab-dashboard

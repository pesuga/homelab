# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system # This name must match the sourceRef in infrastructure.yaml and apps.yaml
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main # Assuming 'main' is your primary branch
  secretRef:
    name: flux-system # Optional: for private repos, points to a secret with deploy key/token
  url: ssh://git@github.com/pesuga/homelab # Using SSH URL, common for deploy keys
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/homelab # Points Flux to the root of your cluster config within the repo
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
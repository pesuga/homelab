apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Keep namespace, Loki, and Promtail
  - namespace.yaml
  - loki-config.yaml
  - loki-pvc.yaml
  - loki-deployment.yaml
  - loki-service.yaml
  - promtail-config.yaml
  - promtail-rbac.yaml
  - promtail-daemonset.yaml
  # Removed: grafana-*, prometheus-*, node-exporter.yaml, kube-state-metrics

# Removed patchesStrategicMerge for prometheus-config-update.yaml

# Middleware - HTTP to HTTPS redirect for LobeChat
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: homelab
spec:
  redirectScheme:
    scheme: https
    permanent: true

apiVersion: v1
kind: Service
metadata:
  name: family-assistant-frontend
  namespace: homelab
  labels:
    app: family-assistant-frontend
    component: frontend
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30300
    protocol: TCP
    name: http
  selector:
    app: family-assistant-frontend

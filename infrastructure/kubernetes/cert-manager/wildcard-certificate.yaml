apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pesulabs-wildcard-cert
  namespace: kube-system
spec:
  secretName: pesulabs-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.pesulabs.net"
    - "*.fa.pesulabs.net"

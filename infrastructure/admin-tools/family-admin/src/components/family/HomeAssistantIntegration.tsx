"use client";

/**
 * Home Assistant Integration - DISABLED
 *
 * This component has been disabled to avoid TypeScript compilation errors.
 * The component is not currently being used in the application.
 *
 * To re-enable this component in the future:
 * 1. Remove this placeholder content
 * 2. Restore the original implementation
 * 3. Fix any TypeScript errors
 */

export default function HomeAssistantIntegration() {
  return (
    <div className="p-6 bg-gray-100 dark:bg-gray-800 rounded-lg">
      <h2 className="text-xl font-semibold text-gray-900 dark:text-white mb-4">
        Home Assistant Integration
      </h2>
      <p className="text-gray-600 dark:text-gray-400">
        This component is currently disabled.
      </p>
    </div>
  );
}